# Cognito Triggers
postConfirmationTrigger:
  handler: functions/triggers/cognito/postConfirmationTrigger.handler
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:PutItem
      Resource:
        - Fn::GetAtt: [UsersTable, Arn]